{
  "name": "verovio-test",
  "version": "1.0.0",
  "description": "Test runner for verovio npm package.",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "verovio": "^6.3.0"
  },
  "devDependencies": {
    "@types/node": "^26.4.0"
  }
}
