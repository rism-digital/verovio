verovio-test
============

Test runner for verovio npm package.

# Usage

- In `emscripten/npm`, do: `npm build && npm link`
- In `emscripten/npm/test`, do: `npm install && npm link verovio && npm test`
